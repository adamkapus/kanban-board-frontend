import React, { useState } from "react";

function ActivityAdder(props) {
  //const [allapot, setAll] = useState(props.kiir);
  return (
    
    <h2 >
      <label >
        Itt lehet felvenni egy új elemet {props.kiir}
      </label>
    </h2>
    
  );
}

export default ActivityAdder;